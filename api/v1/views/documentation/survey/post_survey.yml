    Posts a new survey
    ---
    tags:
      - Surveys
    parameters:
      - name: name
        in: body
        required: true
        requires:
          - name:
        properties:
          name:
            type: string

    responses:
      400:
        description: Missing name or not a JSON
      201:
        description: successfully created survey
